import styled from "styled-components";


export const Logo = styled.span`
  color: #FFFFFF;
  font-weight: bold;
  font-size: 20px;
  letter-spacing: -1px;
  user-select: none;
  flex-shrink: 0;
  transform: skewX(-14deg);
`;